import pathlib
root = pathlib.Path(r"c:\Baocao\fcj-workshop-template")
contents = {
    "content/5-Workshop/_index.vi.md": "---\ntitle : \"Workshop\"\ndate : 2024-01-01 \nweight : 5\nchapter : false\npre : \" <b> 5. </b> \"\n---\n\n#### Giới thiệu tổng quan\n\nPhần Workshop này mô tả cách triển khai dự án GameHub serverless theo dạng CI/CD. Bạn sẽ thực hành từ khởi tạo pipeline đến triển khai frontend, bao gồm cả kiểm thử và giám sát.\n\n#### Hành trình học tập\n\n1. Chuẩn bị công cụ và quyền AWS.\n2. Tạo pipeline với AWS SAM CLI.\n3. Cấu hình repository và stage.\n4. Thêm kiểm thử tự động trong pipeline.\n5. Thiết lập giám sát và logging.\n6. Tạo pipeline trên AWS Console.\n7. Triển khai frontend bằng AWS Amplify.\n\n#### Mục tiêu cuối cùng\n\nKết thúc phần này, bạn sẽ có một hồ sơ triển khai GameHub serverless hoàn chỉnh gồm backend, CI/CD và frontend. Bạn có thể dùng structure này làm nền tảng cho dự án thực tế hoặc báo cáo.\n",
    "content/5-Workshop/5.1-Workshop-overview/_index.vi.md": "---\ntitle : \"Giới thiệu Workshop\"\ndate : 2024-01-01 \nweight : 1\nchapter : false\npre : \" <b> 5.1. </b> \"\n---\n\n#### Mục tiêu Workshop\n\nTrong workshop này, bạn sẽ thực hành thiết lập quy trình CI/CD cho ứng dụng AWS bằng cách sử dụng:\n- AWS SAM để khởi tạo cấu hình pipeline.\n- AWS CodePipeline làm hệ thống CI/CD.\n- GitHub làm nguồn code.\n- AWS Amplify để triển khai frontend.\n\n#### Nội dung chính\n\nBạn sẽ đi qua các bước:\n1. Khởi tạo pipeline cục bộ bằng `sam pipeline init --bootstrap`.\n2. Thiết lập stage Dev và Prod cho ứng dụng.\n3. Kết nối CodePipeline với GitHub qua AWS CodeConnections.\n4. Tạo pipeline trên AWS Console.\n5. Triển khai frontend bằng AWS Amplify.\n6. Thêm bước kiểm thử và giám sát.\n\n#### Về dự án GameHub (serverless)\n\nDự án mẫu là GameHub, một ứng dụng game hub serverless gồm backend Lambda, API Gateway, và frontend web tĩnh. Backend được định nghĩa dưới dạng AWS SAM, còn frontend được host bởi AWS Amplify.\n\n#### Giá trị học được\n\n- Hiểu cách khởi tạo và cấu hình pipeline SAM.\n- Biết định nghĩa stage và ánh xạ stack cho Dev/Prod.\n- Nắm cách kết nối GitHub với AWS CodePipeline.\n- Biết deploy frontend Flutter web trong Amplify.\n- Cài đặt giám sát CloudWatch cơ bản cho serverless app.\n\nNote (hình ảnh): Chèn ảnh mô hình kiến trúc hệ thống tại vị trí này để trực quan (hình: `images/5-Workshop/5.1-Workshop-overview/gamehub-architecture.svg`).\n",
    "content/5-Workshop/5.2-Prerequiste/_index.vi.md": "---\ntitle : \"Chuẩn bị\"\ndate : 2024-01-01 \nweight : 2\nchapter : false\npre : \" <b> 5.2. </b> \"\n---\n\n#### Yêu cầu trước khi bắt đầu\n\nTrước khi thiết lập pipeline và triển khai frontend, bạn cần chuẩn bị:\n- Tài khoản AWS với quyền tạo CodePipeline, CloudFormation, IAM, S3, CodeBuild, CodeCommit, Amplify, CloudWatch và các dịch vụ liên quan.\n- AWS CLI và AWS SAM CLI đã cài đặt và cấu hình.\n- GitHub account và repository chứa mã nguồn.\n- Đường dẫn tới file template SAM/CloudFormation, nhất là nếu bạn dùng monorepo.\n\n#### Kiểm tra ban đầu\n\n1. Mở Terminal tại thư mục gốc của dự án.\n2. Kiểm tra phiên bản AWS CLI và SAM CLI:\n```\naws --version\nsam --version\n```\n3. Cấu hình AWS CLI nếu cần:\n```\naws configure\n```\n4. Kiểm tra profile AWS nếu dùng profile riêng:\n```\naws configure list --profile gamehub\n```\n\n#### Thông tin cần có\n\n- `Region`: ví dụ `ap-southeast-1`.\n- `Repository subfolder`: ví dụ `backend` hoặc `frontend`.\n- `SSM Prefix`: ví dụ `gamehub/` để SAM lưu metadata pipeline.\n- `Git branch`: ví dụ `main` hoặc `V1`.\n- `Pipeline template path`: ví dụ `backend/codepipeline.yaml`.\n- `Stack name` cho Dev/Prod: ví dụ `GameHub-dev`, `GameHub-prod`.\n\n#### Cài đặt nhanh\n\n1. Cài đặt AWS SAM CLI (nếu chưa có):\n```powershell\npip install --user aws-sam-cli\nsam --version\n```\n2. Cài đặt AWS CLI và cấu hình profile workshop (ví dụ `gamehub`):\n```powershell\naws configure --profile gamehub\n```\n3. Nếu bạn sử dụng Windows PowerShell, hãy kiểm tra quyền truy cập bằng cách gọi:\n```powershell\naws sts get-caller-identity --profile gamehub\n```\n\n#### Gợi ý\n\n- Nếu repository là monorepo, tách rõ `backend/` và `frontend/`.\n- Dùng branch riêng cho deploy như `V1` hoặc `main` để dễ kiểm soát.\n- Nếu bạn tổ chức xử lý nhiều môi trường, tạo profile `gamehub-dev` và `gamehub-prod`.\n\nNote (hình ảnh): Thêm ảnh chụp màn hình `aws configure` và `sam --version` tại `images/5-Workshop/5.2-Prerequiste/` để hướng dẫn trực quan.\n",
    "content/5-Workshop/5.3-S3-vpc/_index.vi.md": "---\ntitle : \"Khởi tạo Pipeline SAM\"\ndate : 2024-01-01 \nweight : 3\nchapter : false\npre : \" <b> 5.3. </b> \"\n---\n\n#### Bước 1: Khởi tạo Pipeline\n\n1. Mở Terminal tại thư mục gốc của dự án.\n2. Chạy lệnh:\n```\nsam pipeline init --bootstrap\n```\n\n3. Thực hiện các lựa chọn ban đầu:\n- `Select a pipeline template`: chọn `1` (AWS Quick Start Pipeline Templates)\n- `Select CI/CD system`: chọn `5` (AWS CodePipeline)\n- `Which pipeline template would you like to use?`: chọn `2` (Two-stage pipeline)\n- `Do you want to go through stage setup process now?`: nhập `y`\n\n#### Bước 2: Thiết lập Stage Dev (Stage 1)\n\nKhi hệ thống yêu cầu thông tin cho stage Dev, điền như sau:\n- `Stage configuration name`: nhập `dev`\n- `Select a credential source`: chọn `2` (default named profile)\n- `Enter the region`: nhập mã vùng AWS, ví dụ `ap-southeast-1`\n- Nhập `profile` nếu yêu cầu, ví dụ `gamehub`.\n- Khi hỏi ARN/roles/artifact buckets, nhấn `Enter` để để SAM tạo tự động.\n- `Does your application contain any IMAGE type Lambda functions?`: nhập `n`\n\n#### Bước 3: Thiết lập Stage Prod (Stage 2)\n\n1. Nếu bạn dùng pipeline hai stage, chọn cấu hình cho stage Prod.\n2. `Stage configuration name`: nhập `prod`.\n3. `Select a credential source`: chọn `2` hoặc `1` tùy cách bạn cấu hình profile.\n4. `Enter the region`: giữ cùng region hoặc region khác nếu cần.\n\n#### Bước 4: Kiểm tra file pipeline\n\nSau khi hoàn tất, kiểm tra các file được tạo:\n```bash\nls pipeline\n```\nCác file cần có:\n- `pipeline/samconfig.toml`\n- `pipeline/codepipeline.yaml`\n- `buildspec_deploy.yml`\n\n#### Ghi chú\n\n- `sam pipeline init --bootstrap` sẽ tạo các tài nguyên bootstrap như S3 bucket artifact.\n- Nếu pipeline không thành công, kiểm tra quyền IAM và profile AWS.\n- Bạn có thể chỉnh sửa `samconfig.toml` sau khi hoàn tất để thay đổi tên stack hoặc prefix.\n\n#### Commit và push\n\n```bash\ngit add pipeline/*\ngit commit -m "chore(ci): add sam pipeline config for GameHub"\ngit push origin main\n```\n\nNote (hình ảnh): Chèn ảnh từng bước `sam pipeline init` và file list `ls pipeline/` vào `images/5-Workshop/5.3-S3-vpc/`.\n",
    "content/5-Workshop/5.4-S3-onprem/_index.vi.md": "---\ntitle : \"Cấu hình Repository và Stage\"\ndate : 2024-01-01 \nweight : 4 \nchapter : false\npre : \" <b> 5.4. </b> \"\n---\n\n#### Mục tiêu\n\nPhần này hướng dẫn bạn cấu hình repository và chi tiết stage đã tạo bởi SAM để đảm bảo pipeline vận hành đúng.\n\n#### Các thông tin quan trọng\n\n- `Repository subfolder`: thư mục chứa backend, ví dụ `backend`.\n- `Template path`: đường dẫn tới file SAM/CloudFormation, ví dụ `backend/template.yaml`.\n- `SSM Prefix`: tiền tố để SAM lưu metadata pipeline, ví dụ `gamehub/`.\n- `Stack name` Dev/Prod: ví dụ `GameHub-dev`, `GameHub-prod`.\n- `Branch`: nhánh Git dùng trigger pipeline, ví dụ `main` hoặc `V1`.\n\n#### Thiết lập chi tiết\n\n1. Nếu SAM hỏi `Repository root path`, nhập `backend` (hoặc thư mục tương ứng).\n2. Nếu bạn dùng một repo chứa backend và frontend, hãy chỉ rõ thư mục backend cho phần pipeline.\n3. Kiểm tra phần `template path` đã trỏ đúng tới `backend/template.yaml` hoặc `backend/codepipeline.yaml`.\n\n#### Quản lý stage\n\n- Stage Dev: dùng để kiểm thử nhanh, deploy tự động từ branch develop hoặc main.\n- Stage Prod: dùng để deploy sản phẩm thật sau khi test thành công.\n- Giữ tên stage nhất quán để dễ theo dõi trong AWS CloudFormation và CodePipeline.\n\n#### Lưu ý cấu hình\n\n- `ssm prefix` giúp bạn phân biệt metadata của nhiều pipeline khác nhau.\n- Nếu muốn quản lý nhiều môi trường, tạo các profile AWS tương ứng như `gamehub-dev` và `gamehub-prod`.\n\n#### Gợi ý commit\n\n```bash\ngit add .\ngit commit -m "ci: add sam pipeline configuration for GameHub"\ngit push origin main\n```\n\nNote (hình ảnh): Thêm ảnh `pipeline file list` và `samconfig preview` vào `images/5-Workshop/5.4-S3-onprem/`.\n",
    "content/5-Workshop/5.5a-Testing/_index.vi.md": "---\ntitle : \"Kiểm thử và Pipeline Testing\"\ndate : 2024-01-01\nweight : 5\nchapter : false\npre : \" <b> 5.5. </b> \"\n---\n\n#### Mục tiêu\n\nPhần này hướng dẫn cách thêm bước kiểm thử tự động vào pipeline để phát hiện lỗi trước khi deploy sản phẩm.\n\n#### Nội dung bắt buộc\n\n- Thêm stage `Test` trong pipeline.\n- Dùng AWS CodeBuild để chạy unit test và integration test.\n- Dừng deploy nếu kiểm thử thất bại.\n\n#### Cách cấu hình\n\n1. Tạo hoặc chỉnh sửa file `buildspec_deploy.yml` để bao gồm các lệnh chạy test.\n2. Ví dụ với Python backend:\n```yaml\nversion: 0.2\nphases:\n  install:\n    commands:\n      - pip install -r requirements.txt\n  build:\n    commands:\n      - pytest tests/\nartifacts:\n  files:\n    - '**/*'\n```\n3. Nếu bạn dùng Node.js frontend/test, thêm `npm ci` và `npm test` phù hợp.\n\n#### Lời khuyên\n\n- Tổ chức test theo từng giai đoạn: unit test trước, integration test sau.\n- Dùng output logs từ CodeBuild để xác định nguyên nhân lỗi.\n- Nếu pipeline chạy chậm, chỉ chạy test quan trọng trong stage đầu.\n\n#### Thêm trạng thái
git\n\n- Kết quả test nên hiển thị rõ trong CodePipeline.\n- Nếu dùng GitHub, bạn có thể cấu hình PR check để chỉ merge khi test thành công.\n\nNote (hình ảnh): Thêm ảnh `test-run.png` vào `images/5-Workshop/5.5a-Testing/` để minh họa output test.\n",
    "content/5-Workshop/5.5b-Monitoring/_index.vi.md": "---\ntitle : \"Giám sát & Logging\"\ndate : 2024-01-01\nweight : 6\nchapter : false\npre : \" <b> 5.6. </b> \"\n---\n\n#### Mục tiêu\n\nPhần này mô tả cách thiết lập CloudWatch để giám sát ứng dụng GameHub sau khi deploy, giúp bạn phát hiện lỗi và tình trạng hiệu năng.\n\n#### Cấu hình CloudWatch\n\n- Bật logs cho Lambda function.\n- Bật execution logs cho API Gateway.\n- Tạo metric filter cho lỗi HTTP 5xx, exception, hoặc response time cao.\n- Tạo alarm khi số lỗi vượt ngưỡng.\n\n#### Hướng dẫn nhanh\n\n1. Mở CloudWatch Console.\n2. Vào Logs > Log groups.\n3. Chọn log group của Lambda hoặc API Gateway.\n4. Tạo metric filter từ mẫu lỗi hoặc status code.\n5. Tạo alarm dựa trên metric filter.\n\n#### Gợi ý cảnh báo\n\n- Lambda error rate vượt 1%.\n- API Gateway 5xx rate trên 5% trong 5 phút.\n- Build hoặc deploy pipeline thất bại.\n\n#### Kết nối thông báo\n\n- Dùng SNS để gửi email hoặc Slack khi alarm kích hoạt.\n- Đặt tên alarm rõ ràng, ví dụ `GameHub-Lambda-Error-Alarm`.\n- Kiểm tra định kỳ dashboard CloudWatch sau deploy.\n\nNote (hình ảnh): Thêm ảnh `cloudwatch-alarms.png` vào `images/5-Workshop/5.5b-Monitoring/`.\n",
    "content/5-Workshop/5.5-Policy/_index.vi.md": "---\ntitle : \"Tạo CodePipeline trên AWS Console\"\ndate : 2024-01-01\nweight : 7\nchapter : false\npre : \" <b> 5.7. </b> \"\n---\n\n#### Tạo kết nối GitHub (CodeStar Connections)\n\n1. Mở AWS Console → `Developer Tools` → `Connections`.\n2. Nhấn `Create connection`.\n3. Chọn `GitHub` và xác thực tài khoản.\n4. Chọn repository chứa GameHub.\n5. Kiểm tra trạng thái `Available` sau khi tạo.\n\n#### Tạo pipeline thủ công\n\n1. Mở CodePipeline Console.\n2. Chọn `Create pipeline`.\n3. Chọn `Use a blueprint` hoặc `Start from scratch`.\n4. Nền tảng source: `GitHub (version 2)`.\n5. Chọn connection, repository và branch.\n6. Thêm stage Build với CodeBuild. Sử dụng `buildspec_deploy.yml` trong repo.\n7. Thêm stage Deploy với CloudFormation. Chỉ định `pipeline/codepipeline.yaml` hoặc template phù hợp.\n\n#### Lưu ý khi dùng SAM\n\n- Nếu bạn đã dùng `sam pipeline init`, hãy tái sử dụng `codepipeline.yaml` do SAM tạo.\n- Lưu lại ARN connection để dùng khi tự động hoá.\n- Kiểm tra xem repository path và branch có khớp với pipeline.\n\nNote (hình ảnh): Chụp các màn hình `Create pipeline` và `Connection ARN` vào `images/5-Workshop/5.5-Policy/`.\n",
    "content/5-Workshop/5.6-Cleanup/_index.vi.md": "---\ntitle : \"Triển khai Frontend với AWS Amplify\"\ndate : 2024-01-01\nweight : 8\nchapter : false\npre : \" <b> 5.8. </b> \"\n---\n\n#### Mục tiêu\n\nPhần này hướng dẫn cách kết nối mã nguồn frontend với AWS Amplify và triển khai một ứng dụng web tĩnh từ mã nguồn Flutter.\n\n#### Bước 1: Khởi tạo ứng dụng trên AWS Amplify\n\n1. Truy cập Amplify Console.\n2. Nhấn `Deploy an app`.\n3. Chọn `GitHub` làm source provider.\n4. Xác thực GitHub và chọn repository.\n\n#### Bước 2: Chọn branch và thư mục\n\n1. Chọn branch chứa mã frontend, ví dụ `V1` hoặc `main`.\n2. Nếu repository là monorepo, bật `My app is a monorepo` và nhập `frontend` vào `Monorepo root directory`.\n\n#### Bước 3: Cấu hình build\n\nSử dụng cấu hình dưới đây để build Flutter web trên môi trường Amplify:\n```yaml\nversion: 1\napplications:\n  - frontend:\n      phases:\n        build:\n          commands:\n            - ORIGINAL_DIR=$(pwd)\n            - which xz || sudo yum install -y xz\n            - cd /tmp\n            - wget https://storage.googleapis.com/flutter_infra_release/releases/stable/linux/flutter_linux_3.22.0-stable.tar.xz\n            - tar xf flutter_linux_3.22.0-stable.tar.xz\n            - export PATH="$PATH:/tmp/flutter/bin"\n            - cd $ORIGINAL_DIR\n            - flutter doctor\n            - flutter config --enable-web\n            - flutter channel stable\n            - flutter upgrade\n            - flutter pub get\n            - flutter build web --release --dart-define=COGNITO_REGION=$COGNITO_REGION --dart-define=COGNITO_USER_POOL_ID=$COGNITO_USER_POOL_ID --dart-define=COGNITO_CLIENT_ID=$COGNITO_CLIENT_ID --dart-define=API_GATEWAY_WS_URL=$API_GATEWAY_WS_URL --dart-define=API_GATEWAY_HTTP_URL=$API_GATEWAY_HTTP_URL\n      artifacts:\n        baseDirectory: build/web\n        files:\n          - "**/*"\n      cache:\n        paths: []\n    appRoot: frontend\n```
\n#### Bước 4: Cài đặt biến môi trường\n\nThêm biến môi trường sau trong Amplify Console:\n- `COGNITO_REGION`\n- `COGNITO_USER_POOL_ID`\n- `COGNITO_CLIENT_ID`\n- `API_GATEWAY_HTTP_URL`\n- `API_GATEWAY_WS_URL`\n- `LOCAL_API_URL`\n- `USE_LOCAL_BACKEND`\n- `APK_DOWNLOAD_URL`\n\n#### Bước 5: Review và deploy\n\n1. Nhấn `Next` để xem lại cấu hình.\n2. Kiểm tra kỹ lưỡng branch, root folder và môi trường.\n3. Nhấn `Save and deploy` để bắt đầu deploy.\n\n#### Kết quả\n\nAmplify sẽ tự động tải code từ GitHub, cài Flutter và triển khai frontend dưới dạng ứng dụng web tĩnh.\n\nNote (hình ảnh): Thêm ảnh `amplify-setup.png` vào `images/5-Workshop/5.6-Cleanup/amplify-setup.svg`.\n",
}
for rel, text in contents.items():
    path = root / rel
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(text, encoding='utf-8')
print('Wrote expanded Vietnamese workshop files')
